reports/
*.db-wal
*.db-shm
__pycache__/
.pytest_cache/
.env
data/*.csv
data/*.db
# deploy artifacts: slim mart and trained model are versioned so
# Streamlit Community Cloud can run the app from a fresh clone
!data/olist_mart_slim.db
models/*.pkl
models/*.json
!models/model.pkl
!models/metrics.json
!models/feature_importance.json
!models/drift_reference.json

# root .gitignore blocks *.sql globally; this project versions cleaning SQL
!sql/*.sql

# root .gitignore blocks *.md globally; this project versions its data dictionary
!docs/data-dictionary.md
